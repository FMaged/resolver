# Upstream DNS server to query. Commented out = use the system resolver.
# dns_server = "1.1.1.1"

# Seconds to wait for a reply.
query_timeout = 5

# Retry queries that fail with SERVFAIL (0 disables).
retries = 3
